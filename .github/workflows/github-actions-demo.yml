name: Github Action Demo
on:
  push:
    branches: [main]
    paths-ignore: [".github/**"]
  pull_request:
    branches: [main]
  schedule:
    - cron: '15 6 * * 0'
  workflow_dispatch:
    inputs:
      environment:
        description: "where deploy?"
        type: environement
        required: true
 
jobs:
  Build:
   runs-on: "ubuntu-latest"
   steps:
    - run: |
        echo "event ${{ github.event_name }}"
        echo "branch ${{ github.ref }}"
    - uses: actions/checkout@v4
    - name: List files in the repo
      run: |
          tree
          
